# Optional: set this only when the backend lives on a different origin.
# Leave blank for local/LAN development so the frontend uses same-origin /api calls.
REACT_APP_BACKEND_URL=
ENABLE_HEALTH_CHECK=false
